name: Deploy Site to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ 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 }}  # ← USA O SECRET
        publish_dir: .